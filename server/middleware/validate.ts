export default defineEventHandler((event) => {
  //   擴展 context 物件
  // event.context.userInfo = { user: 'JOJO' };
  // //   驗證請求資料
  // const id = parseInt(event.context!.params!.id) as number;
  // if (!Number.isInteger(id)) {
  //   //   拋出錯誤
  //   return sendError(
  //     event,
  //     createError({
  //       statusCode: 400,
  //       statusMessage: 'ID should be an integer',
  //     }),
  //   );
  // }
});
